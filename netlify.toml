[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-detects Next.js (App Router + API routes) and installs its
# Next.js runtime; no publish dir or plugin declaration needed here.
